- when upload a new paper, send it in file panel without jumping to it
- chat message is showing markdown format. find a library that support converting these text to viewable html
- Chat mode is too restrictive, as if they have the personality of a robot. only fetch data strictly from vector db. maybe because of system prompt and temperature
- reset-password: use style similar to login-card and forgot-password
- when paper is processing, if i delete the paper, the process is expected to stop processing
- when loading PapersyFiles on the FilePanel, only load basic info without the details like summary, key findings, etc. Load the details of the paper once I click on the paper and am taken to the summary page.
- src/lib/components/dedicated/app/types.ts: types should be somewhere else, not in svelte component folder. They should be in src/lib/server but which subfolder?