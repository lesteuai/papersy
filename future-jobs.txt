- Should really switch to a more efficient status polling: WebSocket
- Create requireSession as middleware or put in [all] route to check all routes, instead of each route checks separately.
- Have a dropdown menu at the file panel to change the name of the paper
- How to deal with paper (or PDF) bigger than the context window of the LLM?
- Replace PDF extraction of PDFParse with another library to prevent Retrieve tool from getting garbled output from vector database
- Find some other ways or methods to implement limit number of free users, or just remove it entirely.
```
Option 1 — databaseHooks (closest to your current intent):


export const auth = betterAuth({
  databaseHooks: {
    user: {
      create: {
        before: async (user) => {
          const result = await db.select({ count: count() }).from(userTable);
          if (result[0].count >= MAX_USERS) {
            throw new APIError("BAD_REQUEST", {
              message: "Sign-ups are currently closed (user limit reached)",
            });
          }
        }
      }
    }
  }
});
Option 2 — hooks.before middleware targeting the sign-up endpoint:


import { createAuthMiddleware, APIError } from "better-auth/api";

export const auth = betterAuth({
  hooks: {
    before: createAuthMiddleware(async (ctx) => {
      if (ctx.path !== "/sign-up/email") return;
      const result = await db.select({ count: count() }).from(userTable);
      if (result[0].count >= MAX_USERS) {
        throw new APIError("BAD_REQUEST", {
          message: "Sign-ups are currently closed (user limit reached)",
        });
      }
    }),
  },
});
```